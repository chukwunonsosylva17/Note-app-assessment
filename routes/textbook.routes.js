const express = require('express');
const router = express.Router();
const noteController = ('./')

// create a notebook router
router.post('/add', (req,res) =>{
    console.log(req.body)
} )

module.exports = router;